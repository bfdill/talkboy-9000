{
  "name": "@talkboy-9000/player",
  "version": "0.0.1",
  "license": "Unlicense",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "clean": "rimraf dist",
    "test": "jest --no-cache",
    "watch": "yarn build --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@talkboy-9000/sounds": "^0.0.1"
  },
  "devDependencies": {
    "@talkboy-9000/models": "^0.0.1",
    "@talkboy-9000/winston-jest": "^0.0.1"
  }
}
